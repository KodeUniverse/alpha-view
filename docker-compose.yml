version: '3.9'

services:
  alpha-view-server:
    build:
      context: .
      dockerfile: server.Dockerfile
    ports:
      - "1337:1337"
  
  finviz-scraper:
    build:
      context: .
      dockerfile: finviz-scraper.Dockerfile
  
  redis:
    image: redis:8-bookworm